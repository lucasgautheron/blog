sudo: true
cache:
    pip: true

language: node_js

python:
 - '3.4'

before_install:
  - sudo apt-get -y install language-pack-fr imagemagick python3-pip python-dev
  - sudo pip3 install -U setuptools
  - sudo pip3 install -U virtualenvwrapper
  - python3 -V
  - pip3 -V

install:
 - pip3 install Nikola
 - pip3 install "Nikola[extras]"
 - npm install -g firebase-tools

script:
 - ./build.sh && nikola build

after_success:
 - bash deploy.sh
# - test $TRAVIS_PULL_REQUEST == "false" && test $TRAVIS_BRANCH == "master" && bash deploy.sh
